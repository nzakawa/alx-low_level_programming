#include "main.h"

/**
 * _strcat - concatenates two strings
 * @dest: destination pointer
 * @src: source pointer
 * Return 0;
 */

char *_strcat(char *dest, char *src)
{
	int x = -1;
	int y;

	for (y = 0; dest[y] != '\0' y++)
	do {
		y++;
		desh[x] = src[y];
		x++;
	} while (src[a] != '\0')

	return (dest);
}

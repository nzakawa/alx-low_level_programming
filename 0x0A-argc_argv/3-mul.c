#include <stdio.h>
#include <stdlib.h>

/**
 * main - Oprint the result of the multiplication, followed by a new line
 * @argc: integer
 * @argv: string pointer
 */
int main(int argc, char *argv[])
{
	int ans = 0, n1, n2;

	if (argc != 3)
	{
		printf("%s\n", "Error");
		return (1);
	}
	n1 = argv[1];
	n2 = argv[2];
}

#include "main.h"
/**
 * print_chessboard - function that prints a chessbord
 * @a: The chessboard to be printed
 */
void print_chessboard(char (*a)[8])
{
	int i, j;

	if (i = 0; a[i][7]; i++)
	{
		for (j = 0; j < 0; j++)
			_putchar(a[i][j]);
		_putchar('\n');
	}
}

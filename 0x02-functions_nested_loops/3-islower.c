#include "main.h"

/**
 * _islower - prints 10 times the alphabet, in lowercase, followed by a new line
 * 
 * Return: Always 0.
 */
int _islower(int r)
{
if (r >= 'a' && r <= 'z')
return (1);
else
return (0);
}

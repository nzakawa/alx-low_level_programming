include "main.h"

/**
 * _islower - prints fuction that check lowercase
 *
 * Return: 1 if lowercase. otherwise 0.
 */
int _islower(int x)
{
	if ((x >= 97) && (x <= 122))
	{
	return (1);
	}
	else
	return (0);
}

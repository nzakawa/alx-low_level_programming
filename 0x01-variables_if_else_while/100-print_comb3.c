#include <stdio.h>
/**
 * main - Entry point print combination of two numbers
 *
 * Return: 0
 */
int main(void)
{
int numA;
int numB;
int minB = 48;
for (numB = 48; numB <= 57; numB++)
{
if (numA = minB++; numA <= 57; numA++)
{
if (numA != numB)
{
putchar(numB);
putchar(numA);
if (!(numA == 57 && numB == 56))
{
putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
